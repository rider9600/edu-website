## RECAP 

- _F_ P 

- Probability space (Ω _, ,_ ) 

- Axioms of probability 

- Sigma-algebra’s as domain _F_ 

- Borel-Sigma algebras 

- Conditional probability 

- Law of total probability & Bayes rule 

46 / 80 

## Independence 

- Independence of events 

- Mutually exclusive events 

- Conditional independence 

# ▶ Zero probability events and independence 

- Counting (HW) 

- Motivate Random variables 

47 / 80 

## Independence 

- Consider the experiment of tossing a coin and a dice simultaneously. 

- its . 

- Identify underlying probability space 

- What is P _H_ 6 ( _{ , }_ )? 

- What is P _T_ odd P _T i_ ( _{ , }_ ) = ( _{∪i_ =1 _,_ 3 _,_ 5 _{ , }}_ )? 

- In both cases above we have P _A ∩ B P A P B_ ( ) = ( ) ( ). 

- This implies that P( _A/B_ ) = P( _A_ ). 

   - Two events _A B_ are _P A B P A ,_ independent iff ( _/_ ) = ( ) 

   - and _P_ ( _B/A_ ) = _P_ ( _B_ ). 

   - Two events _A B_ are _,_ independent iff 

      - _P A ∩ B P A P B_ ( ) = ( ) ( ). 

48 / 80 

## Independence 

   - Two events _A B_ are if and if _,_ independent only 

   - _P A ∩ B P A P B_ ( ) = ( ) ( ). 

- If _A_ and _B_ are then are _A[c]_ and _B[c]_ independent, independent? 

- What about _A_ and _B[c]_ ? Are they independent? 

- If _A A A_ are then that 1 _,_ 2 _, . . . , n_ independent, prove 

_n P ∪[n] − P A_ ( _i_ =1 _[A][i]_[) = 1] _[ −]_ Q [1 ( _i_ )] _i_ =1 

49 / 80 

## Mutual and Pairwise Independence 

- A collection of events _{Ai , i ∈ I}_ are said to be **mutually** if the _P ∩ A_[Q][for][any][subset] _[J]_ 

- **independent** ( _j∈J j_ ) = _j∈J[P]_[(] _[A][j]_[)] of _I._ 

- A collection of events _{Ai , i ∈ I}_ are said to be **pairwise** if of events from the collection are 

- **independent** any pair independent. 

- Mutual independence implies pairwise independence but not the other around. way 

- HW: Find an example where pairwise independence does not imply mutual independence. 

50 / 80 

## - Independence Example 

- Pick a number from the set 1 10 . randomly _{ , . . . , }_ 

- Event _A_ that the number is less than 7. says 

▶ Event _B_ that the number is less than 8. says 

- Event _C_ says that the number is even. 

- Are the events mutually independent? 

- Which pair of event is independent? 

51 / 80 

## Correlation between events 

- Two events _A B_ are _P A ∩ B P A P B ,_ independent iff ( ) = ( ) ( ). 

- Two events _A_ and _B_ are positively correlated iff _P A B > P A_ ( _/_ ) ( ). 

- Two events _A_ and _B_ are negatively correlated iff 

   - _P A B < P A_ ( _/_ ) ( ). 

- _A_ and _B_ have the same correlation as _A[c]_ and _B[c]_ . 

- _A_ and _B_ have the opposite correlation as _A_ and _B[c]_ . 

52 / 80 

## Mutually exclusive and Independence 

- Two events _A_ and _B_ are mutually exclusive if occurrence of one implies that the other event cannot occur. Are they independent? 

- If _A_ and _B_ are mutually exclusive, then they are not independent (and vice versa). This can be seen as follows. 

## **A and B are Mutually Exclusive** 

## **A and B are Independent** 

▶ _P A ∩ B_ ( ) = 0 

- _P A ∩ B P A P B_ ( ) = ( ) ( ) 

▶ _P_ ( _A/B_ ) = 0 

▶ _P_ ( _A/B_ ) = _P_ ( _A_ ) 

**==> picture [288 x 35] intentionally omitted <==**

▶ _P_ ( _A/B[c]_ ) = _P_ ( _A_ ) 

- If _A B_ then two events are neither exclusive nor _⊆ ,_ mutually 

- independent. 

53 / 80 

## Zero probability events and Independence 

Zero events are probability always independent! 

▶ Let _E_ be a zero probability event, i.e. _P_ ( _E_ ) = 0. 

- Then for any set _F_ , we want to show that _P_ ( _E ∩ F_ ) = 0 _._ 

- Note that _E ∩ F ⊆ E ._ 

- This implies that _P_ ( _E ∩ F_ ) _≤ P_ ( _E_ ). 

54 / 80 

## Conditional independence 

▶ Recall : _P_ ( _A/B_ ) = _[P] P_[(] ( _[AB] B_ )[)][.] 

- Also recall : _P_ ( _A/BC_ ) = _[P] P_[(] ( _[AB] B/[/] C[C]_ )[)] 

- This implies _P_ ( _AB/C_ ) = _P_ ( _A/BC_ ) _P_ ( _B/C_ ). 

Two events _A_ and _B_ are said to be conditionally independent of event _C P C >_ if _P AB C P A C .P B C_ ( ( ) 0) (( ) _/_ ) = ( _/_ ) ( _/_ ) 

▶ As a _P A BC P A C_ consequence ( _/_ ) = ( _/_ ) HW: Verify if events _A_ and _B_ are conditionally independent of event _C_ (in the experiment of picking number randomly in 1 10 _{ , .., }_ ) 

55 / 80 

## – Conditional independence example 

- There are two coins, one fair and other fake (both heads). The experiment is to choose a coin uniformly and toss twice. 

- Event A: First coin toss results in H. What is its probability? = 

- P(A) 3/4. 

- Event B: Second coin toss results in H. What is its = 

- probability? P(B) 3/4. 

- Event C: Coin 1 is chosen. 

- What is _P_ ( _A/C_ ) and _P_ ( _B/C_ )? 1 _/_ 2 

- What is _P A ∩ B C_ 1 4 Hence A and B are (( ) _/_ )? _/_ 

- conditionally independent given C. 

- Are A and B independent? HW 

56 / 80 

## First principle of counting 

57 / 80 

## Principle of counting 

▶ Given _n_ objects, in how many ways can you arrange them? n! 

**==> picture [587 x 56] intentionally omitted <==**

**==> picture [644 x 57] intentionally omitted <==**

- Note that in each combination or group of _k_ objects, the ordering within each group is immaterial. What if we also want to count this? 

_n n_ ▶ _P_ = _C × k_ ! _k k_ 

58 / 80 

## Experiments with Sampling 

- Sampling: Sampling from a set means choosing an element from the set. 

- Sampling uniformly at random: All items in the set have equal probability of being chosen. 

- Sampling can be with replacement or without replacement. 

- Sampling can be ordered or unordered. 

- In ordered _a b c c b a_ sampling, ( _, ,_ ) _̸_ = ( _, ,_ ). 

- This leaves us with 4 combinations. 

   1. Ordered sampling with replacement 

   2. Ordered sampling without replacement 

   3. Unordered sampling with replacement 

   4. Unordered sampling without replacement 

59 / 80 

## Ordered sampling with replacement 

▶ Suppose you want to sample _k_ out of _n_ objects with replacement and where the ordering of the _k_ objects matters. ▶ Because we sample with replacement, repetition is allowed. 

- How can choose _k_ out of _n_ this many ways you objects way? 

▶ a) _nk_ ? b) � _kn_ � c) _k[n]_ d) _n[k]_ ? 

- There are _k_ positions and _n_ choices for every position. 

- Total _n[k]_ . 

60 / 80 

## Ordered sampling without replacement 

- Suppose you want to sample _k_ out of _n_ objects now without replacement and where the ordering of the _k_ objects matters. 

- Because we sample without replacement, repetition is not allowed. 

- How can choose _k_ out of _n_ this many ways you objects way? 

**==> picture [449 x 28] intentionally omitted <==**

- There are _k_ positions and _n − i_ + 1 choices for every _i[th]_ position. 

**==> picture [558 x 35] intentionally omitted <==**

61 / 80 

## Unordered sampling without replacement 

- Here you want to sample _k_ out of _n_ objects without replacement and the ordering of the _k_ objects does not matters. 

- Because we sample without replacement, repetition is not allowed. 

- How can choose _k_ out of _n_ this many ways you objects way? 

**==> picture [449 x 28] intentionally omitted <==**

- Essentially we want to count distinct _k_ sized subsets from _n_ objects without caring for ordering. 

▶ _nC_ . _k_ 

62 / 80 

## Unordered sampling with replacement 

- Here you want to sample _k_ out of _n_ objects with replacement and the ordering of the _k_ objects does not matters. 

- Because we sample with replacement, repetition is allowed. 

- How can choose _k_ out of _n_ this many ways you objects way? 

- In such _i_ can atmost _k_ times. any sampling, any object appear 

- Let _x_ denote the number of times _i_ is chosen in _k i_ object 

- samples. 

_n_ ▶ Then _x_ = _k_ any sampling satisfies P _i i_ =1 

- How many solutions to the above equation tells you how can do the above 

- many ways you sampling. 

▶[�] _[n]_[+] _[k][−]_[1] _k_ � Think(HW). 

63 / 80 

## Example 1 

▶ How many different 7-plate licenses are possible if the first 3 places are to be occupied by letters and the final 4 by numbers? 

- ANS: 26[3] 10[4] . 

- What if the alphabets and numbers are not to repeat? 

64 / 80 

## Example 2 

- How many functions defined on _n_ points are possible if each functional value is either 0 or 1? 

- ANS: 2 _[n]_ 

65 / 80 

## Example 3 

- How letter can be formed many different arrangements using 

- the letters PEPPER? 

- If the P’s and E’s are as _P P P_ and _E E R_ distinguished 1 _,_ 2 _,_ 3 1 _,_ 2 _,_ 

- then 6!. 

- But we don’t want to distinguish the P’s and E’s. 

- For every indistinguishable arrangement, say PPPREE, there are 3! _×_ 2! different distinguished arrangements. 

- Using principles of counting, the number of indistinguishable 6! 

- arrangements are 3!2!1! 

66 / 80 

## Example 4 

- How many different permutations of _n_ objects can be formed when _n_ 1 are alike, _n_ 2 are alike, ..., _nr_ are alike? 

**==> picture [385 x 33] intentionally omitted <==**

**==> picture [530 x 33] intentionally omitted <==**

▶ Now there are _n_ distinct items and want to divide suppose you them in _r_ where _i_ has size _n_ and where groups group _i r_[How][many][ways][can][you][do][this][in?] P _i_ =1 _[n][i]_[=] _[ n]_[.] 

**==> picture [387 x 31] intentionally omitted <==**

**==> picture [280 x 33] intentionally omitted <==**

67 / 80 

## Example 5 

- There are _n_ red balls and _r_ bins. How many ways can you put these balls in bins such that no bin is empty ? 

- ANS: This is same as the number of solutions to finding 

- _r_ 

- P _xi_ = _n_ where _xi >_ 0. 

- _i_ =1 

- Arrange all _n_ balls in a line. 

- There are _n −_ 1 spaces between these _n_ balls where you want _−_ 

- to place _r_ 1 partitions (or sticks). 

- No two partitions can be in the same space else that would mean a bin is empty. 

- 

- ▶ Select _r_ 1 out of _n −_ 1 (unordered without replacement) 

▶[�] _[n][−]_[1] . _r −_ 1� 

68 / 80 

## Example 6 

- There are _n_ red balls and _r_ bins. How many ways can you put these balls in bins such that bins can be empty ? 

▶ ANS: This is same as the number of solutions to finding _r_ P _xi_ = _n_ where 0 _≤ xi ≤ n_ . _i_ =1 

▶ This is same as the number of solutions to finding _r_ P _yi_ = _n_ + _r_ where 0 _< yi < n_ . (substitute _yi_ = _xi_ + 1 _i_ =1 above!) 

▶[�] _[n]_[+] _[r][−]_[1] . _r −_ 1 � 

69 / 80 

## – Example 6 Alternative solution 

- There are _n_ red balls and _r_ bins. How many ways can you put these balls in bins such that bins can be empty ? 

▶ ANS: This is same as the number of solutions to finding _r_ P _xi_ = _n_ where 0 _≤ xi ≤ n_ . _i_ =1 

- _x_ that vertical lines. 

- Represent _i_ by many 

- 

- ▶ Total _n_ vertical lines and _r_ 1 + signs. 

- _−_ 

- ▶ _n_ + _r_ 1 objects where _n_ are alike and _r_ 1 are alike. 

**==> picture [114 x 32] intentionally omitted <==**

70 / 80 

## Example 7 

- Toss a biased coin _n_ times with as the of head. _p_ probability 

- What is the probability that you have _k_ heads ? 

_n k n−k_ ▶ ANS: _−_ . � _k_ � _p_ (1 _p_ ) 

- When _p_ = 1 and _k_ = _n_ , we will have the convention that 0[0] = 1!. Check the link following 

- `https: //en.wikipedia.org/wiki/Zero_to_the_power_of_zero` 

- What is the probability that you will get head for the first time at the _r[th]_ toss where _r ≤ n_ ? 

- ANS: _−_ . (1 _p_ ) _[r][−]_[1] _p_ 

71 / 80 

## Example 8 

- Suppose you roll a dice _n_ times, what is probability that half of them show 1 and remaining half show 6? (n is even) 

_n n_ 1 ▶ ANS:� _n/_ 2�[�] 6 � 

- _n_ of them show 1 and _n_ show 6? 

- What is the probability that 1 2 

**==> picture [425 x 46] intentionally omitted <==**

72 / 80 

## Example: Monty Hall 

- There are 3 doors, 2 goats and 1 car. 

- Please choose one door! Suppose you choose Door 1. 

- As a presenter, I open a door which has a goat (say door 2) and give you an option to change your choice. Will you ? 

- Sol: Let _C_ denote the event that door _i_ conceals a car and _G i_ 

- denote the event that a goat is shown at door 2. 

- _P_ ( _G_ ) = 1 since the presenter has shown a goat behind door 2. 

- What is _P C G_ if the is correct. ( 3 _/_ )? Verify following 

- 1[)] _[P]_[(] _[C]_ 1 _[c]_[)] 

- ▶ _P_ ( _C_ 3 _/G_ ) = _[P]_[(] _[C]_[3] _[∩][G][|][C]_[1][)] _[P]_[(] _[C]_[1] _P_[)][+] ( _G[P]_ )[(] _[C]_[3] _[∩][G][|][C][c]_ = 2 _/_ 3 _> P_ ( _C_ 3) 

73 / 80 

## Motivation to random variables 

73 / 80 

## Random variable 

- Given a random with associated _F_ P it is experiment (Ω _, ,_ ) _,_ 

- sometimes to deal with _ω ∈_ Ω. a difficult directly eg. rolling 

- dice ten times. 

- Notice that each _ω ∈_ Ωis not a number but a sample point 

- of numbers. 

- sequence 

- Also, we may be interested in functions of these sample points rather than samples themselves. eg: Number of times 6 in the 10 rolls. 

- appears 

- In either case, it is often convenient to work in a new _simpler_ rather than the 

- probability space original space. 

- Random variable is a device which precisely helps us make this from _F_ P to a _F[′] P_ 

- mapping (Ω _, ,_ ) ’simpler’ (Ω _[′] , , X_ ). 

- _PX_ is called as an induced probability measure on Ω _[′]_ . 

74 / 80 

## Random variable as a measurable function 

   - A random variable _X_ is a function _X_ : Ω _→_ Ω _[′]_ that transforms the _F_ P to _F[′] P_ and is probability space (Ω _, ,_ ) (Ω _[′] , , X_ ) 

   - _F F[′]_ . 

   - ‘( _,_ )-measurable’ 

- The map _X_ : Ω _→_ Ω _[′]_ implies _X_ ( _ω_ ) _∈_ Ω _[′]_ for all _ω ∈_ Ω. 

- For event _B ∈F[′]_ , the pre-image _X[−]_[1] ( _B_ ) is defined as _X[−]_[1] _B ω ∈_ Ω: _X ω ∈ B_ ( ) := _{_ ( ) _}_ 

The ‘( _F, F[′]_ )-measurability’ implies that for every _B ∈F[′]_ , we have _X[−]_[1] ( _B_ ) _∈F_ . 

75 / 80 

